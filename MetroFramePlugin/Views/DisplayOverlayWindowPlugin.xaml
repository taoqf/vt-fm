<controls:VicWindowNormal x:Class="MetroFramePlugin.Views.DisplayOverlayWindowPlugin"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
        xmlns:controls="http://schemas.ifeidao.com/wpfcontrol"
        xmlns:vm="clr-namespace:MetroFramePlugin.ViewModels"
        Title="活动插件" Name="DisplayOverlayWindow" Height="350" Width="900" Style="{DynamicResource WindowMessageSkin}" WindowStartupLocation="CenterScreen">
    <Window.DataContext>
        <vm:DisplayOverlayWindowPluginViewModel/>
    </Window.DataContext>
        <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding displayOverlayWindowPluginCommand}" CommandParameter="{Binding ElementName=DisplayOverlayWindow}"/>
        </i:EventTrigger>
    </i:Interaction.Triggers>
    <!--上下显示-->
    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="400"></RowDefinition>
            <RowDefinition Height="20"></RowDefinition>
        </Grid.RowDefinitions>
    <controls:VicStackPanelNormal Orientation="Vertical"  Name="statePanel" Grid.Row="0">


        </controls:VicStackPanelNormal>
        <Button Content="&#xe603;" IsEnabled="{Binding UpPageVis}" Style="{DynamicResource btnNextPageStyle}" Command="{Binding btnUpPageClickCommand}" Margin="10,283,802,65" ></Button>
        <Button Content="&#xe622;" IsEnabled="{Binding NextPageVis}" Style="{DynamicResource btnNextPageStyle}" Command="{Binding btnNextPageClickCommand}" Background="Transparent" Margin="68,283,744,65"></Button>
    </Grid>-->
    <!--左右显示-->
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40"></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition Width="40"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Button Content="&#xe6ab;" IsEnabled="{Binding UpPageVis}" Height="40" Width="40" HorizontalAlignment="Center" VerticalContentAlignment="Center" Grid.Column="0" Style="{DynamicResource btnNextPageStyle}" Command="{Binding btnUpPageClickCommand}" Background="Transparent"></Button>
        <controls:VicStackPanelNormal Grid.Column="1" Orientation="Vertical"  Name="statePanel">


        </controls:VicStackPanelNormal>
        
        <Button Content="&#xe6ae;" IsEnabled="{Binding NextPageVis}" Height="40" Width="40" HorizontalAlignment="Center" VerticalContentAlignment="Center" Grid.Column="2" Style="{DynamicResource btnNextPageStyle}" Command="{Binding btnNextPageClickCommand}" Background="Transparent"></Button>
    </Grid>
</controls:VicWindowNormal>
