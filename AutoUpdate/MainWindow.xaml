<Window x:Class="AutoUpdate.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="自动更新"  Name="updateWindow"   Height="180" Width="525"   ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Window.Resources>
        <SolidColorBrush x:Key="RightBackground" Color="White" Opacity="0.6"/>
        <Style TargetType="{x:Type TextBlock}">
            <Setter Property="Background" Value="#D8D8D8"/>
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>
        <Style TargetType="{x:Type ProgressBar}">
            <Setter Property="Background" Value="Gainsboro"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Value" Value="20"/>
            <Setter Property="BorderBrush" Value="#D8D8D8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ProgressBar}">
                        <Grid MinHeight="14" MinWidth="400">
                            <Border Name="PART_Track"  CornerRadius="20"  BorderBrush="Black"  BorderThickness="1" Background="#FF57C7C9" />
                            <Border x:Name="PART_Indicator"  CornerRadius="16" BorderBrush="Red" BorderThickness="1"  HorizontalAlignment="Left">
                                <Border.Background>
                                    <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                                        <GradientStop Color="Orange" Offset="0"/>
                                        <GradientStop Color="Yellow" Offset="0.5"/>
                                        <GradientStop Color="Orange" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Opacity="0.6">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Grid.RowSpan="5" Background="Honeydew" />
        <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left" Name="tBlockProcess" Margin="0,12"/>
        <ProgressBar Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Left" Name="proBarTotal" Margin="0,18" Grid.RowSpan="2"/>
        <TextBlock Grid.Row="3" VerticalAlignment="Center" HorizontalAlignment="Left" Name="tBlockNow" Text="正在下载:" Margin="0,12"/>
        <ProgressBar Grid.Row="4" VerticalAlignment="Center" HorizontalAlignment="Left" Name="proBarNow" Margin="0,18"/>
    </Grid>
</Window>
