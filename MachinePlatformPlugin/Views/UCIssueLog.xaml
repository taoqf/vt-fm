<UserControl x:Class="MachinePlatformPlugin.Views.UCIssueLog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
             xmlns:controls="clr-namespace:Victop.Wpf.Controls;assembly=Victop.Wpf.Controls"
             xmlns:vm="clr-namespace:MachinePlatformPlugin.ViewModels"
             xmlns:components="clr-namespace:Victop.Frame.Components;assembly=Victop.Frame.Components"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="400" Name="IssueLogView">
    <controls:VicGridNormal>
        <Grid.DataContext>
            <vm:IssueLogViewModel></vm:IssueLogViewModel>
        </Grid.DataContext>
 
        <!--页面初始化-->
        <i:Interaction.Triggers>
            <i:EventTrigger EventName="Loaded">
                <i:InvokeCommandAction Command="{Binding IssueLogViewLoadedCommand}" CommandParameter="{Binding ElementName=IssueLogView}"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
        <!--主界面分为2行-->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <!--主界面第一行-->
        <RichTextBox Grid.Row="0" x:Name="tboxIssueLog" IsReadOnly="True" MinHeight="200" ScrollViewer.VerticalScrollBarVisibility="Auto" Margin="2.5,5"/>
    </controls:VicGridNormal>
</UserControl>
